Модуль должен содержать модельку для хранения ссыолк на категории и на товары
а так же на портфолио и спецпредложения - все, что раскручивали (на каждую сущность отдельная моделька)

Алгоритм по категориям:

т.к. url совпадают для категорий, то мы сначала идем в основную вьюху нашу, там
мы ищем категорию, если не находим, томы должны слазить в модельку old_urls.category
и поискать там (т.е. сделаем редирект на вьюшку именно данного old_urls) ??? пока не знаю,
как это сделать не через общий диспатчер
если там нашла, отправляется в номральную категорию по нормальному адресу, если нет, на 404


Алгоритм по товарам:

все, что приходит в шаблоне старого сайта по товару, сразу идет на вьюшку по товару old_urls.product
, там ищется соответствиеи перенаправляется на новую сылку по продукту


По остальным пока не подошло время еще